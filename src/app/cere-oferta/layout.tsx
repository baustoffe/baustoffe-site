import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cere o Ofertă — Baustoffe",
  description: "Solicită o ofertă personalizată pentru uși și ferestre PVC la dimensiuni speciale.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
